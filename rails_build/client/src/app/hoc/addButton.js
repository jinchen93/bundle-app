import React from 'react';

export default props => {
  return (
    <Button
      block={true}
      bsStyle="success"
      type="submit"
      className="sidebar__wrapper__input__add"
    >
      +
    </Button>
  );
};
